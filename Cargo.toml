[package]
name = "lbright"
version = "0.1.0-rc1"
edition = "2021"

[dependencies]
# Zero external crate dependencies for standard library core

[profile.release]
opt-level = 2
lto = true
codegen-units = 1
strip = true

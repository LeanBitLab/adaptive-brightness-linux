[package]
name = "lbright"
version = "2.0.0"
edition = "2021"

[dependencies]
# Zero external crate dependencies for standard library core

[profile.release]
opt-level = 2
lto = true
codegen-units = 1
strip = true
